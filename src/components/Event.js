// import { Events, Body, Sleeping } from "matter-js";
export const eventCollisionStart = (current) => {
  // Events.on(current, "collisionStart", (event) => {
  //   const source = event.source;
  //   const arrayPair = source.detector.pairs;
  //   const { bodyA, bodyB } = arrayPair.collisionStart[0];
  //   const vx = bodyB.velocity.x;
  //   const vy = bodyB.velocity.y;
  //   console.log("==>");
  //   if (bodyA.label === "RectFilter1" && bodyB.label === 1) {
  //     Body.setVelocity(bodyB, { x: vx - 3, y: vy });
  //   }
  //   if (bodyA.label === "RectFilter1" && bodyB.label === 2) {
  //     Body.setVelocity(bodyB, { x: vx + 3, y: vy });
  //   }
  // });
};
